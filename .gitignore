# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Pytest
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Claude
.claude/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Data / DB
backend/data/*.db
backend/data/*.sqlite
backend/data/*.db-journal
# WAL mode leaves these two alongside the DB; they are live scratch state and
# meaningless without it, so they must never be committed.
backend/data/*.db-wal
backend/data/*.db-shm

# Persisted terrain cache (DATA_DIR defaults to backend/ for local runs)
backend/cache/

# Model/buffer snapshots taken before a rebuild
*.bak-*
*.contaminated
# Buffers set aside by load() when the feature width changed
*.npz.v*
*.npz.unreadable

# Secrets
.env
.env.*
# ...but the templates must be committed — they hold no secrets
!.env.example
!**/.env.example
*.key
*.pem
